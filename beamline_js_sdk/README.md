# beamline_js_sdk

TypeScript SDK that gives JavaScript developers access to Beamline websocket capabilities.

This package provides:

- Client lifecycle management
- Topic publish/subscribe
- Request/response with timeout handling
- Mock transport for local development and demo flows

See `docs/api-design.md` for the API contract.
